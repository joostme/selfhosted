version: "3.4"
services:
  adguard:
    image: adguard/adguardhome:latest
    restart: unless-stopped
    networks:
      - traefik
    ports:
      - "53:53/udp"
      - "53:53/tcp"
    volumes:
      - work:/opt/adguardhome/work
      - conf:/opt/adguardhome/conf
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.adguard.rule=Host(`adguard.home`)"
      - "traefik.http.routers.adguard.service=adguard"
      - "traefik.http.services.adguard.loadbalancer.server.port=80"

      - "traefik.http.routers.adguard2.rule=Host(`adguard.home`)"
      - "traefik.http.routers.adguard2.service=adguard2"
      - "traefik.http.services.adguard2.loadbalancer.server.port=3000"
  
networks:
  traefik:
    external: true

volumes:
  work:
  conf:
services:
  airconnect:
    network_mode: host
    image: 1activegeek/airconnect:1.10.1
    volumes:
      - ${APPDATA_PATH}/airconnect:/config
    restart: unless-stopped
    labels:
      glance.name: AirConnect